<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atqgh.micro.security.mapper.SecMenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.atqgh.micro.acsApi.entity.Menu">
        <id column="menu_id" property="menuId" />
        <result column="menu_name" property="menuName" />
        <result column="parent_id" property="parentId" />
        <result column="order_num" property="orderNum" />
        <result column="path" property="path" />
        <result column="component" property="component" />
        <result column="is_frame" property="isFrame" />
        <result column="is_cache" property="isCache" />
        <result column="menu_type" property="menuType" />
        <result column="visible" property="visible" />
        <result column="status" property="status" />
        <result column="perms" property="perms" />
        <result column="icon" property="icon" />
        <result column="create_by" property="createBy" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="update_time" property="updateTime" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        menu_id, menu_name, parent_id, order_num, path, component, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, update_by, update_time, remark
    </sql>

    <select id="getMenusByUserId" parameterType="java.lang.Long" resultMap="treeMap">
        select
            sm.menu_id ,
            sm.menu_name,
            sm.parent_id,
            sm.order_num,
            sm.path,
            sm.component,
            sm.is_frame,
            sm.is_cache,
            sm.menu_type,
            sm.visible,
            sm.`status`,
            sm.perms,
            sm.icon,
            sm.remark,
            create_time
        from
            sys_menu sm
        inner join
            sys_role_menu srm
        on
            sm.menu_id = srm.menu_id
        inner join
            sys_user_role sur
        on
            srm.role_id = sur.role_id
        where sur.user_id = #{userId}
    </select>

    <resultMap id="treeMap" type="com.atqgh.micro.acsApi.entity.MenuVo">
        <id column="menu_id" property="menuId" />
        <result column="menu_name" property="menuName" />
        <result column="parent_id" property="parentId" />
        <result column="order_num" property="orderNum" />
        <result column="path" property="path" />
        <result column="component" property="component" />
        <result column="is_frame" property="isFrame" />
        <result column="is_cache" property="isCache" />
        <result column="menu_type" property="menuType" />
        <result column="visible" property="visible" />
        <result column="status" property="status" />
        <result column="perms" property="perms" />
        <result column="icon" property="icon" />
        <result column="create_time" property="createTime" />
        <result column="remark" property="remark" />
        <collection column="menu_id" property="subMenuList" select="getVoByParentId" />
    </resultMap>

    <select id="getMenusById" parameterType="java.lang.Long" resultMap="treeMap">
        select
            sm.menu_id,
            sm.menu_name,
            sm.parent_id,
            sm.order_num,
            sm.path,
            sm.component,
            sm.is_frame,
            sm.is_cache,
            sm.menu_type,
            sm.visible,
            sm.`status,
            sm.perms,
            sm.icon,
            sm.remark,
            create_time
        from
            sys_menu sm
        where sm.menu_id = #{menuId}
    </select>

    <select id="getVoByParentId" parameterType="java.lang.Long" resultMap="treeMap">
        select
            sm.menu_id,
            sm.menu_name,
            sm.parent_id,
            sm.order_num,
            sm.path,
            sm.component,
            sm.is_frame,
            sm.is_cache,
            sm.menu_type,
            sm.visible,
            sm.`status`,
            sm.perms,
            sm.icon,
            sm.remark,
            create_time
        from
            sys_menu sm
        where sm.parent_id = #{parentId}
    </select>

</mapper>
