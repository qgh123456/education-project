<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atqgh.micro.security.mapper.SecUserMapper">

    <select id="selectPermissionByUserId" resultType="com.atqgh.micro.acsApi.entity.Menu" parameterType="java.lang.Long">
        select
            sm.menu_id as menuId,
            sm.menu_name as menuName,
            sm.perms as perms
        from
            sys_user_role sur
        inner join
            sys_role_menu srm
        on
            sur.role_id = srm.role_id
        inner join
            sys_menu sm
        on srm.menu_id = sm.menu_id
        where sur.user_id = #{id}
    </select>

</mapper>
